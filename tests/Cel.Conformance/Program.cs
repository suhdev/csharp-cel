// Conformance harness entry point. Will load the cel-spec textproto corpus and dispatch to
// the public CEL façade once Compile/Eval are available. For now this just prints a banner so
// the build target produces a runnable artifact.

Console.WriteLine("Cel conformance harness — not yet implemented.");
return 0;
